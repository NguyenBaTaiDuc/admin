import NotFound from "@/components/NotFound/NotFound";
import React from "react";

const NotFoundPage = () => {
  return (
    <div>
      <NotFound />
    </div>
  );
};

export default NotFoundPage;
